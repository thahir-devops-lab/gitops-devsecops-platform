apiVersion: v1
kind: Service
metadata:
  name: gitops-platform-service
  labels:
    app: gitops-platform

spec:
  type: NodePort

  selector:
    app: gitops-platform

  ports:
    - name: http
      port: 80
      targetPort: 5000